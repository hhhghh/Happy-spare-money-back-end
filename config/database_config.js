// var mysql      = require('mysql');

const config = {
    host     : '139.196.79.193',
    user     : 'root',
    password: '123456',
    database : 'hhhghh'
}

// var connection = mysql.createConnection({
    // host     : '139.196.79.193',
    // user     : 'root',
    // password: '123456',
    // database : 'hhhghh'
// });

module.exports = config

// connection.connect();

// // test query, never mind ...

// connection.query('SELECT * FROM hhhghh.team', function (error, results, fields) {
    // if (error) throw error;
    // console.log('The solution is: ', results[0]);
// });
